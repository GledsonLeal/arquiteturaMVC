{{#each erros}}
    <div class="alert alert-danger">{{texto}}</div>
{{else}}
{{#each acertos}}
    <div class="alert alert-success">{{texto}}</div>
{{else}}

{{/each}}

{{/each}}
<h1>Lista de Alunos: </h1>

{{#each alunos}}  
  <div class="form-row">
    
    <div class="form-group col-md-6">
      <label for="nome">Nome: </label>
      <p class="alert alert-secondary">{{this.nome}}</p>
    </div>
    <div class="form-group col-md-6">
      <label for="email">Email</label>
      <p class="alert alert-secondary" >{{this.email}} </p>
    </div>
  </div>
  <div class="form-group">
    <label for="endereco">Endereço</label>
    <p class="alert alert-secondary" >{{this.endereco}} </p>
  </div>
  <div class="form-row">
    <div class="form-group col-md-6">
      <label for="cidade">Cidade</label>
      <p class="alert alert-secondary" >{{this.cidade}} </p>
    </div>
    <div class="form-group col-md-4">
      <label for="estado">Estado</label>
      <p class="alert alert-secondary" >{{this.estado}} </p>
      </div>
    <div class="form-group col-md-2">
      <label for="cep">CEP</label>
      <p class="alert alert-secondary" >{{this.cep}} </p>
    </div>
  </div>
  
  <div class="form-group">
        <div class="form-row">
          <div class="form-group col-md-1">
                <form action="/alunos/deletarAluno" method="post">
                    <input type="hidden" name="id", value="{{_id}}">
                    <button class="btn btn-danger">Deletar</button></a>     
                </form>
          </div>
          <div class="form-group col-md-1">
              <form action="/alunos/editarAluno" method="post">
                  <input type="hidden" name="id", value="{{_id}}">
                  <button class="btn btn-primary">Editar</button></a> 
              </form>  
                <!--<a href= "/alunos/editarAluno/{{_id}}"><button class="btn btn-primary">Editar</button></a>-->
          </div>
        </div>
  </div>
 
  <hr>
{{else}}
    <h5>Nenhum aluno cadastro</h5>
{{/each}}